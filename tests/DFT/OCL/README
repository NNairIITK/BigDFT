To use GPU capabilities, you should:
- use the option --enable-cuda with configure
- Add at the first line of input.dat:
CUDAGPU  this keyword indicates that the special GPU interface is used for convolutions
- Have a file for the affinity:
cpu_gpu_aff.config
